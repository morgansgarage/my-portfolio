version : 0.1

phases:
    build:
      commands:
        -echo Nothing to do stylesheet
artifacts:
    files:
        - index.HTML
        - favicon.ico
        - Resume.pdf
        - styles/main.CSS
        - images/*
